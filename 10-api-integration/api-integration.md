---
description: Write a Python script to integrate two APIs
---

Write a simple Python script that connects the following two services. Requirements:
- Use the `requests` library
- Handle authentication for both APIs
- Include the core data flow (get from one, send to the other)
- Add simple error handling
- Use clear variable names so a beginner can understand it

No extra features — just make it work cleanly.

Integration needed (e.g. "Get new Typeform submissions → create Notion entries"):
$ARGUMENTS
